# Cleaning-Unleaned-Data-Set-
This mini project focuses on data cleaning, manipulation, and analysis using Python Pandas. Missing values were handled, data types were corrected, and new columns were created. Grouping and aggregation techniques were applied to generate insights and convert raw data into a structured, analyzable dataset.
